# Пошаговая инструкция: Как найти Base Configuration File

## Способ 1: Через поиск (САМЫЙ ПРОСТОЙ)

1. Вы уже в **Build Settings** (это видно на скриншоте)
2. В правом верхнем углу панели Build Settings есть **поле поиска "Filter"** с лупой 🔍
3. **Введите в поиск:** `baseConfiguration` (без кавычек)
4. Нажмите Enter
5. Должна появиться настройка **"Base Configuration File"**

## Способ 2: Переключите режим отображения

1. В Build Settings, под вкладками, есть кнопки: **"Basic"**, **"Customized"**, **"All"**, **"Combined"**, **"Levels"**
2. Сейчас у вас выбрано **"Basic"** (видно на скриншоте)
3. **Нажмите на "All"** - это покажет ВСЕ настройки, включая скрытые
4. Прокрутите вниз до секции **"Packaging"**
5. Внутри секции "Packaging" найдите **"Base Configuration File"**

## Способ 3: Через секцию Packaging

1. В Build Settings прокрутите вниз до секции **"Packaging"**
2. В этой секции вы увидите:
   - Info.plist File
   - Product Bundle Identifier
   - Product Name
   - **Base Configuration File** ← Вот оно!

## Если всё равно не видно:

Попробуйте:
1. Нажмите на кнопку **"Levels"** (вместо "Basic")
2. Это покажет настройки по уровням: Project, Target, Resolved
3. Найдите **"Base Configuration File"** в колонке "Convertik" (Target уровень)

## Что должно быть в Base Configuration File:

- Для конфигурации **DeployOld**: `Configs/DeployOld.xcconfig`
- Для конфигурации **DeployNew**: `Configs/DeployNew.xcconfig`  
- Для конфигурации **Debug**: `Configs/Debug.xcconfig`

## Альтернатива: Проверьте через терминал

Если не можете найти в Xcode, проверьте через терминал:

```bash
cd frontend/ios
xcodebuild -showBuildSettings -workspace Convertik.xcworkspace \
  -scheme Convertik-Old -configuration DeployOld \
  | grep -i "baseConfiguration"
```

Если выводит пусто - значит xcconfig не подключен.



