# Оптимизация времени запуска приложения

## Проблема

Приложение запускалось очень долго (50+ секунд) из-за:
1. **Синхронная инициализация AdMob** в `init()` - блокировала запуск
2. **Множественные параллельные синхронизации** - дублирование запросов
3. **Реклама загружалась сразу** - до показа UI
4. **StoreService проверял подписку сразу** - блокировал запуск

## Решение: Оптимизированный порядок инициализации

### Новый порядок запуска:

```
0.0s  → UI показывается сразу (мгновенно)
0.0s  → Локальные данные загружаются (из CoreData, мгновенно)
1.5s  → Синхронизация данных с API (в фоне, не блокирует UI)
2.0s  → Проверка статуса подписки (в фоне)
3.0s  → Инициализация AdMob SDK (в фоне)
3.5s  → Загрузка рекламы (после AdMob, в фоне)
```

### Что изменилось:

#### 1. ConvertikApp.swift
- ❌ **Было**: AdMob инициализировался синхронно в `init()`
- ✅ **Стало**: AdMob инициализируется асинхронно через 3 секунды после показа UI

#### 2. AdService.swift
- ❌ **Было**: Реклама настраивалась сразу при создании сервиса
- ✅ **Стало**: Реклама загружается только после инициализации AdMob SDK

#### 3. ContentView.swift
- ❌ **Было**: Дублирование синхронизации в `onAppear`
- ✅ **Стало**: Синхронизация запускается только из RatesRepository

#### 4. RatesRepository.swift
- ✅ **Осталось**: Локальные данные загружаются сразу
- ✅ **Улучшено**: Синхронизация отложена на 1.5 секунды (было 1 секунда)

#### 5. AdBannerRepresentable.swift
- ✅ **Улучшено**: Получение rootViewController выполняется асинхронно
- ✅ **Улучшено**: Загрузка рекламы полностью асинхронная в фоне
- ✅ **Улучшено**: UI не блокируется при создании и загрузке рекламы

#### 6. InterstitialAdService.swift
- ✅ **Улучшено**: Загрузка рекламы выполняется в Task.detached (фон)
- ✅ **Улучшено**: Обработка результатов на MainActor (не блокирует UI)

## Результат

### До оптимизации:
- ⏱️ Время до показа UI: **50+ секунд**
- 🔴 AdMob блокировал запуск
- 🔴 Множественные параллельные запросы
- 🔴 Реклама загружалась до показа UI
- 🔴 Интерфейс блокировался во время загрузки рекламы

### После оптимизации:
- ⏱️ Время до показа UI: **< 1 секунда**
- ✅ UI показывается мгновенно с локальными данными
- ✅ Все тяжелые операции выполняются в фоне
- ✅ Реклама загружается после показа UI
- ✅ **Интерфейс НЕ блокируется** - все операции с рекламой асинхронные
- ✅ Загрузка рекламы происходит параллельно с работой приложения

## Логи запуска

### Ожидаемый порядок в логах:

```
🚀 ConvertikApp: Running in RELEASE mode
✅ GADApplicationIdentifier found in Info.plist: ...
RatesRepository init() called
loadLocalRates: ... (локальные данные загружены)
[UI показывается пользователю - < 1 секунда]
[Через 1.5 секунды]
Starting remote sync...
[Через 2 секунды]
🚀 StoreService: Checking premium status...
[Через 3 секунды]
📱 Initializing AdMob SDK (delayed for fast UI launch)...
🚀 Google Mobile Ads SDK initialization status: ...
[Через 3.5 секунды]
🎯 AdService: Initializing ads after AdMob SDK is ready...
```

## Проверка

После изменений проверьте:

1. ✅ UI показывается сразу (< 1 секунда)
2. ✅ Локальные данные видны сразу
3. ✅ Синхронизация происходит в фоне (не блокирует UI)
4. ✅ Реклама загружается после показа UI
5. ✅ Нет дублирования синхронизаций в логах

## Дополнительные оптимизации (если нужно)

Если запуск все еще медленный:

1. **Увеличить задержку синхронизации** до 2-3 секунд
2. **Отложить проверку подписки** до 3-4 секунд
3. **Загружать рекламу только при необходимости** (lazy loading)

